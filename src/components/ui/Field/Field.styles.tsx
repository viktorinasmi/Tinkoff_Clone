import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    borderRadius: 16,
    backgroundColor: 'rgba(172,190,199,0.24)',
    marginTop: 12,
    padding: 14,
  },
});
