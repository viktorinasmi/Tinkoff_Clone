import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  itemContainer: {
    width: 56,
    height: 56,
    borderRadius: 12,
    marginHorizontal: 16,
    overflow: 'hidden',
  },
  itemGradient: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },

  itemText: {
    fontSize: 12,
    lineHeight: 16,
    textAlign: 'center',
    marginTop: 6,
  },
});
