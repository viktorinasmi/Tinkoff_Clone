import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    width: '100%',
    fontWeight: 'bold',
    color: 'rgba(0,0,0,.8)',
    fontSize: 24,
    lineHeight: 32,
  },

  textCenter: {
    textAlign: 'center',
  },
});
