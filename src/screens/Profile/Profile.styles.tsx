import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerSuccess: {
    backgroundColor: '#00b92d',
    padding: 12,
    borderRadius: 8,
  },
  successText: {
    color: '#fff',
    textAlign: 'center',
  },
});
